The program uses:

- External Merge Sort
- Multiprocessing
- K-way merge with heap

It can process files larger than available RAM.

Python program for sorting huge `.txt` integer files using external merge sort and multiprocessing.

python sort.py random_numbers.txt 1000000